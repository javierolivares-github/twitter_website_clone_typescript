import MoonButton from "./MoonButton";

/**The **MoonButton** component renders a button with the icon of a moon. */
export default {
  title: 'Atoms/Buttons/MoonButton',
  component: MoonButton,
  tags: ['autodocs'],
}

/**This is the default version of **MoonButton** component: */
export const Default = {
  args: {}
}