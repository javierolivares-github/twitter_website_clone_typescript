import IconButtonEvents from "./IconButtonEvents";

/**The **IconButtonEvents** component renders a button with the icon of an event inside. */
export default {
  title: 'Atoms/IconButtons/IconButtonEvents',
  component: IconButtonEvents,
  tags: ['autodocs'],
}

/**This is the default version of **IconButtonEvents** component: */
export const Default = {
  args: {}
}