import IconButtonLocation from "./IconButtonLocation";

/**The **IconButtonLocation** component renders a button with the icon of a location inside. */
export default {
  title: 'Atoms/IconButtons/IconButtonLocation',
  component: IconButtonLocation,
  tags: ['autodocs'],
}

/**This is the default version of **IconButtonLocation** component: */
export const Default = {
  args: {}
}