import HomeHeaderInput from "./HomeHeaderInput";

/**The **HomeHeaderInput** component renders an input field. */
export default {
  title: 'Atoms/Inputs/HomeHeaderInput',
  component: HomeHeaderInput,
  tags: ['autodocs'],
}

/**This is the default version of **HomeHeaderInput** component: */
export const Default = {
  args: {
    
  }
}