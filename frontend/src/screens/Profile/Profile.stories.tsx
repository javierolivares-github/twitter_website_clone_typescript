import Profile from './Profile';

/** The **Profile** component renders the Profile page. */
export default {
  title: 'Screens/Profile',
  component: Profile,
  tags: ['autodocs'],
}

/**This is the default version of Profile component:*/
export const Default = {
  args: {},
}